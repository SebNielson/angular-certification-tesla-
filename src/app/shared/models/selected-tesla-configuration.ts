import {TeslaConfigurationOptions} from "./tesla-configuration-options";

export interface SelectedTeslaConfiguration {
  config?: TeslaConfigurationOptions,
  towHitch?: boolean,
  yoke?: boolean,
}
